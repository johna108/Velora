"use client";

/**
 * The Genkit AIProvider is deprecated in v1.x and no longer needed.
 * This component is now a no-op and can be safely removed from your application.
 */
export function GenkitAIProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
